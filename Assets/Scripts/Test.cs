using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Test : MonoBehaviour
{
    // Quaternion.LookRotation(Vector3.forward, Vector3.up);    // Top Face
    // Quaternion.LookRotation(Vector3.forward, Vector3.right); // Right Face
    // Quaternion.LookRotation(Vector3.down, Vector3.back);     // Back Face
    
    // Start is called before the first frame update
    void Start()
    {
        
    }
}
